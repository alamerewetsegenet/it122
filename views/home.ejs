<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
</head>
<body>
    <ul>
        <% data.forEach(item => { %>
            <li>
                <a href="/detail?name=<%= item.name %>"><%= item.name %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>