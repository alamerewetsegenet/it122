<!DOCTYPE html>
<html>
    <body>
        <title>Detail</title>
 <% if (item) { %>  
    <h2><%= item.make %></h2>
    <li> <%= item.model %></li>
    <li> <%= item.year %></li>
    <li> <%= item.color %></li>
    <li> <%= item.price %></li>
<a href="/">Back to home</a>
 <% } else { %> 
        <h2><%= item %> not found</h2>
<% } %> 

    </body>
</html>